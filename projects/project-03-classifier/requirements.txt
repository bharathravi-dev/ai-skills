# Project 3 dependencies. Versions verified installed and imported 2026-09-08,
# Python 3.12.3.  [EXECUTED]
#
# Install (from this directory):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -e ".[dev]"
#
# The src/ layout means an editable install is REQUIRED -- see M2-L06.

numpy==2.5.3
pytest==9.1.1
